package bdv.viewer.state.r;

/**
 * A SourceGroup handle.
 * Identifies the same source group over time (while both the group's name and associated sources change).
 */
public final class SourceGroup
{}
